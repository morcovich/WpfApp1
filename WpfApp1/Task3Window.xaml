<Window x:Class="WpfApp1.Task3Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Задание 3: Проверка диапазона" Height="200" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="Введите число (0-100):" Margin="0,0,10,0"/>
            <TextBox x:Name="textBoxNumber" Width="100"/>
        </StackPanel>

        <Button Grid.Row="1" Content="Проверить" Click="ButtonCheck_Click" Margin="0,10,0,0" HorizontalAlignment="Center"/>

        <TextBlock x:Name="textBlockResult" TextWrapping="Wrap" Grid.Row="2" TextAlignment="Center" FontSize="14" Margin="0,10,0,0"/>
    </Grid>
</Window>
